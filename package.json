{
  "name": "maintenance-worker",
  "version": "1.0.0",
  "description": "Terraform maintenance worker module project",
  "scripts": {
    "build": "webpack",
    "format": "prettier --write  '*.{json,js}' 'src/**/*.{js,ts}'",
    "lint": "eslint --max-warnings=0 src && prettier --check '*.{json,js}' 'src/**/*.{js,ts}'",
    "test": "jest"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@cloudflare/workers-types": "latest",
    "ts-loader": "latest",
    "typescript": "latest"
  },
  "devDependencies": {
    "@types/jest": "latest",
    "@types/node": "latest",
    "eslint": "latest",
    "jest": "latest",
    "prettier": "latest",
    "webpack": "latest",
    "webpack-cli": "latest"
  }
}
